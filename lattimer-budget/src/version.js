'use strict';

// The build revision, reported by the API so phones can tell when they are
// running an old copy of the app and refresh themselves. Keep in step with
// APP_VERSION in public/app.js and VERSION in public/sw.js.
module.exports = 21;
