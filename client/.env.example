# Base URL of the Railway API, with no trailing slash.
# Leave empty for local development so the Vite dev proxy handles /api.
VITE_API_BASE=https://your-service.up.railway.app

# Local development only: where the dev proxy forwards /api requests.
VITE_DEV_API=http://localhost:8080
